#include <stdio.h>
#include <stdlib.h>
#include <string.h>
int main() {
    int n,p;
    char p[100];
    while (1) {
        scanf("%d",&n);
        if(n==0)
            break;
        for(i=0;i<n;i++)
        {
            scanf("%s",s[i][100000000000]);
        }
        n = atoi(p);
        printf("%d", n);
    }
    return 0;
}
