#include <bits/stdc++.h>
#define ll long long int
using namespace std;
int main() {
    string s, s1;
    cin >> s >> s1;
    ll n, n1, j, i,sum;
    n = s.length();
    n1 = s1.length();
    i=n-1;
    j=n1-1;
    while(i!=0 || j!=0)
    {
       sum=s[i]+s1[j];
    }
    return 0;
}
